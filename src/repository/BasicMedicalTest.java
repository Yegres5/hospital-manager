package repository;

import clinic_staff.DoctorTitle;

public class BasicMedicalTest extends MedicalTest {
    public BasicMedicalTest(DoctorTitle doctorTitle, Float cost, String name) {
        super(doctorTitle, cost, name);
    }
}
